import { Link } from "react-router-dom"; import { buildArticlePath } from "../utils/articleRoutes"; import ArticleImage from "./ArticleImage";
function ArticleCard({ article, sectionKey, compact = false }) { return (<article className={`article-card ${compact ? "compact" : ""}`}><ArticleImage article={{ ...article, imageHeight: compact ? 220 : article.imageHeight || 220 }} alt={article.title} className="article-card-image" variant="fixed" /><div className="article-card-body"><p className="article-meta"><span>{article.category || article.source}</span><span>•</span><span>{article.date}</span></p><h4>{article.title}</h4><p>{article.summary}</p><div className="card-actions"><Link to={buildArticlePath(sectionKey, article.id)} className="card-link">Ver noticia</Link>{article.externalUrl ? <a href={article.externalUrl} target="_blank" rel="noreferrer" className="card-link subtle">Fuente original</a> : null}</div></div></article>); }
export default ArticleCard;