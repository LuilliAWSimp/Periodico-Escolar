import { Link } from "react-router-dom"; import ArticleImage from "./ArticleImage";
function ArticleDetail({ article, sectionLabel, backTo }) { if (!article) return null; return (<article className="article-detail"><div className="article-detail-header"><div className="article-detail-topbar"><Link to={backTo} className="back-link">← Volver a {sectionLabel}</Link><p className="article-meta article-detail-meta"><span>{article.category || article.source}</span><span>•</span><span>{article.date}</span><span>•</span><span>{article.author || article.source}</span></p></div><h2>{article.title}</h2><p className="article-detail-summary">{article.summary}</p></div><ArticleImage article={{ ...article, imageHeight: article.imageHeight || 460 }} alt={article.title} className="article-detail-image-wrapper" variant="detail" /><div className="article-detail-body"><p>{article.content || article.summary}</p>{article.externalUrl ? <div className="detail-source-box"><span>Consulta también la cobertura original publicada por la fuente.</span><a href={article.externalUrl} target="_blank" rel="noreferrer" className="source-link">Abrir fuente original</a></div> : null}</div></article>); }
export default ArticleDetail;