import { useState } from "react"; import { useNavigate } from "react-router-dom"; import { authenticateAdmin, createAdminSession } from "../utils/storage";
function AdminLogin({ onLogin }) { const navigate = useNavigate(); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const handleSubmit = (event) => { event.preventDefault(); if (!authenticateAdmin(username, password)) { setError("Credenciales incorrectas. Revisa usuario y contraseña."); return; } createAdminSession(); onLogin(); navigate("/admin", { replace: true }); }; return (<section className="admin-login-shell"><div className="admin-login-card"><p className="section-eyebrow">Acceso editor</p><h2>Panel de administración escolar</h2><p className="admin-copy">Desde aquí la persona encargada puede crear, editar y eliminar noticias escolares y configurar la identidad del periódico sin tocar el código.</p><form className="admin-form" onSubmit={handleSubmit}><label>Usuario<input type="text" value={username} onChange={(event) => setUsername(event.target.value)} placeholder="admin" /></label><label>Contraseña<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="editor2026" /></label>{error ? <p className="form-error">{error}</p> : null}<button type="submit" className="primary-button">Iniciar sesión</button></form><div className="admin-help-box"><strong>Credenciales por defecto</strong><span>Usuario: admin</span><span>Contraseña: editor2026</span><span>Se pueden cambiar desde el archivo .env.</span></div></div></section>); }
export default AdminLogin;