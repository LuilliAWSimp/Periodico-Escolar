function ArticleImage({ article, alt, className = "", variant = "card" }) { const fit = article.imageFit || "cover"; const position = article.imagePosition || "center center"; const height = article.imageHeight || (variant === "detail" ? 440 : 220); const wrapperStyle = variant === "fixed" || variant === "detail" ? { height: `${height}px` } : undefined; return (<div className={`article-image-shell ${variant} ${className}`.trim()} style={wrapperStyle}><img src={article.image} alt={alt} className="article-image" style={{ objectFit: fit, objectPosition: position }} /></div>); }
export default ArticleImage;